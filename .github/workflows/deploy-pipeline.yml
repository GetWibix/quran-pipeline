name: Deploy Pipeline

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Run deploy script
        working-directory: /var/www/quran-pipeline
        run: bash deploy.sh
